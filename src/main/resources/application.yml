# Server - đổi port nếu 8080 bị chiếm (chỉ chạy 1 instance bot để tránh lỗi 409)
server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/milktea_db
    username: postgres
    password: ${DB_PASSWORD:123}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

# Long Polling Bot
telegram:
  bot:
    username: ${TELEGRAM_BOT_USERNAME:milktea_bot}
    token: ${TELEGRAM_BOT_TOKEN:8631106923:AAFaygkOy_OjRnKVdNYjg2TiSlGX7nON0NI}